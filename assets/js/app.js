// import Vue from 'vue';
import { createApp } from 'vue'
import Example from './components/Example.vue'

const app = createApp(Example).mount('#app')


// map = L.map('map').setView([51.505, -0.09], 13)